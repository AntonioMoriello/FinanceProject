@model BudgetDetailsViewModel

@{
    ViewData["Title"] = "Budget Details";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8">
            <div class="card shadow mb-4">
                <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                    <h4 class="card-title mb-0">@Model.Budget.Name</h4>
                    <div>
                        <a asp-action="Edit" asp-route-id="@Model.Budget.BudgetId"
                           class="btn btn-light btn-sm">
                            <i class="fas fa-edit"></i> Edit
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    <div class="row mb-4">
                        <div class="col-md-6">
                            <h5>Budget Information</h5>
                            <dl class="row">
                                <dt class="col-sm-4">Category</dt>
                                <dd class="col-sm-8">@Model.Budget.Category.Name</dd>

                                <dt class="col-sm-4">Period</dt>
                                <dd class="col-sm-8">@Model.Budget.Period</dd>

                                <dt class="col-sm-4">Start Date</dt>
                                <dd class="col-sm-8">@Model.Budget.StartDate.ToShortDateString()</dd>

                                <dt class="col-sm-4">End Date</dt>
                                <dd class="col-sm-8">@Model.Budget
